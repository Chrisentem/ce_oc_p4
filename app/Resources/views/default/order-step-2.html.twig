{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div id="header" class="jumbotron">
            <h1>Billetterie du Louvre</h1>
        </div>
        
        <div class="row">
            <div id="content" class="col-md-6">
                <h3>enregistrement des visiteurs</h3>
                    <div class="well">
                    {{ form_start(form) }}
                    {{ form_rest(form)}}
                    <a href="{{ path('homepage') }}" class="btn btn-primary">Retour</a>
                    <button type="submit" class="btn btn-primary">étape suivante</button>
                    {{ form_end(form)}}
                    </div>
            </div>
        </div>

    </div>
{% endblock %}

{% block stylesheets %}
<style>
</style>
{% endblock %}
