{% extends 'base.html.twig' %}

{% block title %}
    {{ 'page.title.contact_confirmation'|trans }} - {{ parent() }}
{% endblock %}
{% block breadcrumb %}
{% endblock %}
{% block body %}
    <div class="row">
        <div id="content" class="col-lg-12">
            <div class="jumbotron" role="alert">
                <p>{{ 'contact_confirmation.p1'|trans }}</p>
            </div>
        </div>
        <div class="col-lg-12">
            <a href="{{ path('homepage') }}" class="btn btn-primary">{{ 'button.back_home'|trans }}</a>
        </div>
    </div>
{% endblock %}